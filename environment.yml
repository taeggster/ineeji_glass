name: ineeji_glass
channels:
  - conda-forge
dependencies:
  - python=3.13
  - pandas=2.3.*
  - pyarrow=21.*
  - ipykernel
  - matplotlib
  # tigramite runtime deps (conda-forge builds beat pip wheels for these)
  - numpy
  - scipy
  - numba
  - networkx
  - scikit-learn
  - pip
  - pip:
      - tigramite
